---
name: uversion
description: Show plugin version
disable-model-invocation: true
---

Identify the root directory of the plugin (PLUGIN_ROOT), set cwd to the uspecs-using project root, and run `bash {PLUGIN_ROOT}/bin/softeng.sh action uversion [options]` and follow the instructions in the output.

